# Application dependencies

require:
    - play  [1.2,)
#    - play -> morphia 1.2.4b
    - play -> router 1.1

    - cmscore -> cmscore 0.1
#    - stoopid -> stoopid 0.1

repositories:
    - localModules:
        type: local
        artifact: "${application.path}/custom/[module]/"
        contains:
            - cmscore
#            - stoopid