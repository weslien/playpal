# Application dependencies

require:
    - play [1.2,)
    - cmscore -> cmscore 0.1

repositories:
    - localModules:
        type: local
        artifact: "${application.path}/../[module]/"
        contains:
            - cmscore
