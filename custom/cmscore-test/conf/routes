# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Modules
*       /                                       module:cmsadmin
*       /                                       module:cmscore

# Home page
GET     /                                       cmscore.CmsCoreApplicationMock.index
GET     /{identifier}                           cmscore.CmsCoreApplicationMock.page
GET     /{identifier}/{<[0-9]+>version}         cmscore.CmsCoreApplicationTest.pageVersion

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}
