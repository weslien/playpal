self: play -> cmscore 0.1

require:
    - play [1.2,)

repositories:
    - localModules:
        type: local
        artifact: "${application.path}/../[module]/"
        contains:
            - cmstheme-default
